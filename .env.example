# Database connection string (Vercel Postgres/Neon)
POSTGRES_URL=
# Optional alias if your provider gives DATABASE_URL instead
DATABASE_URL=

# Vercel Blob token for server-side uploads/deletes
BLOB_READ_WRITE_TOKEN=
